user daemon
pidfile /var/tmp/sniproxy.pid
listener <%= node.default["dns-proxy"][:local_ip_addr] %> 80 {
    proto http
}
listener <%= node.default["dns-proxy"][:local_ip_addr] %> 443 {
    proto tls
}
table {
    domain\.com *
}
